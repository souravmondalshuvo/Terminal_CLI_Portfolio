/**
 * Canonical list of available themes. The `theme` command and
 * any future theme-picker UI both source from here.
 *
 * To add a theme:
 *   1. Add its name to THEMES below.
 *   2. Add a `.terminal[data-theme="<name>"] { ... }` block in
 *      styles/themes.css with the overridden CSS variables.
 *
 * The 'default' theme is special: it means "no data-theme attribute".
 */
export const THEMES = ['default', 'amber', 'matrix', 'paper'];
export const DEFAULT_THEME = 'default';
